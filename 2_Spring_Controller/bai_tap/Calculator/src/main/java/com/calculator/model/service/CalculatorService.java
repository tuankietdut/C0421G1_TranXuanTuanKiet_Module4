package com.calculator.model.service;

public interface CalculatorService {
    double plus (double firstNumber, double secondNumber);
    double except (double firstNumber, double secondNumber);
    double multiple (double firstNumber, double secondNumber);
    double divide (double firstNumber, double secondNumber);

}
